version: "3.1"

services:
  alimentacao:
    image: postgres:15.3-alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
    ports:
        - 5432:5432